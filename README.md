# Soveldaja kassa frontend

## Build docker image 

``` docker build . --tag soveldaja-kassa-front ```
This image will be used in the backend docker-compose file

## Run locally 
``` npm i ``` to install packages
``` npm run dev ``` to run locally 